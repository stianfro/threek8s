# Production values override for specific deployment
# This file should NOT be committed to the repository
# Use with: helm upgrade threek8s ./helm/threek8s -f helm/values-production.yaml

frontend:
  env:
    VITE_API_URL: "https://threek8s-api.apps.aa-sre-64uhyc.clusters.intilitycloud.com/api"
    VITE_WS_URL: "wss://threek8s-api.apps.aa-sre-64uhyc.clusters.intilitycloud.com/ws"

backend:
  env:
    CORS_ORIGINS: "https://threek8s.apps.aa-sre-64uhyc.clusters.intilitycloud.com"